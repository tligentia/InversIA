import type { StockData, Period, Asset } from '../types';

// This service is intentionally not providing data generation functions
// to ensure the application relies on external, real-data sources for charting,
// as per user requirements. The `generateAssetData` function has been removed.
